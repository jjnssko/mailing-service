<form method="post" action="{{ path('app_email_process_log_delete', {'id': email_process_log.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ email_process_log.id) }}">
    <button class="btn">Delete</button>
</form>
