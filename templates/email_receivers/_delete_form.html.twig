<form method="post" action="{{ path('app_email_receivers_delete', {'id': email_receiver.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ email_receiver.id) }}">
    <button class="btn">Delete</button>
</form>
