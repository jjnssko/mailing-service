{% extends 'layout.html.twig' %}

{% block title %}EmailReceivers{% endblock %}

{% block content %}
    <h1>EmailReceivers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ email_receiver.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ email_receiver.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_email_receivers_index') }}">back to list</a>

    <a href="{{ path('app_email_receivers_edit', {'id': email_receiver.id}) }}">edit</a>

    {{ include('email_receivers/_delete_form.html.twig') }}
{% endblock %}
